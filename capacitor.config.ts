import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vscvietnamslingshot.varcheck',
  appName: 'VSC VAR Check',
  webDir: 'dist'
};

export default config;
